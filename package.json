{
  "name": "cinedrive-root",
  "version": "1.0.0",
  "description": "CineDrive Monorepo Root Package",
  "main": "server/index.js",
  "scripts": {
    "start": "cd server && npm install && node index.js"
  }
}
